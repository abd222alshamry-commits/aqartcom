# عقارتكم V13 — البحث السعري بالعملة المختارة

## الجديد
- فلاتر الحد الأدنى/الأقصى للسعر تعمل بالعملة التي اختارها العميل من `displayCurrency`.
- API `/api/properties` يقبل `currency=USD|EUR|SAR|...`.
- النتائج تعيد `price_display` و`display_currency` بالإضافة إلى السعر الأصلي و`currency`.
- التحويل يتم على مستوى استعلام PostgreSQL اعتماداً على جدول `currency_rates`، بحيث لا يتم تقطيع النتائج بعد LIMIT.
- تغيير عملة العرض يعيد تحميل نتائج البحث ويحدث تلميح العملة بجانب حقول السعر.
- نقاط الخريطة تعرض السعر بالعملة المختارة.

## مثال
`GET /api/properties?currency=SAR&minPrice=500000&maxPrice=1500000`

هذا يعني: البحث عن عقارات تتراوح أسعارها بين 500 ألف و1.5 مليون ريال سعودي، بغض النظر عن العملة الأصلية للعقار.
