const qs=new URLSearchParams(location.search);const slug=qs.get('slug');const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function load(){if(!slug){$('#app').innerHTML='<div class="error">رابط المكتب غير صحيح.</div>';return}try{const r=await fetch('/api/public/offices/'+encodeURIComponent(slug));const d=await r.json();if(!r.ok)throw new Error(d.error);const o=d.office;document.title=o.name+' | عقارتكم';$('#verified').innerHTML=o.verified?'<span class="verified">✓ مكتب موثّق</span>':'';$('#app').innerHTML=`<section class="cover"><div class="cover-inner"><div class="logo">${o.logo_url?`<img src="${esc(o.logo_url)}" style="width:100%;height:100%;object-fit:cover;border-radius:25px">`:'⌂'}</div><div><h1>${esc(o.name)} ${o.verified?'<span class="verified">موثّق</span>':''}</h1><p>${esc(o.description||'مكتب عقاري متخصص في البيع والشراء والإيجار.')}</p><div class="meta"><span>🏠 ${o.properties_count} عقار</span><span>👥 ${o.staff_count} موظف</span><span>📍 ${esc(o.city||'')}</span><span>${esc(o.district||'')}</span></div><div class="actions">${o.phone?`<a class="btn" href="tel:${esc(o.phone)}">اتصال</a>`:''}${o.whatsapp?`<a class="btn dark" target="_blank" href="https://wa.me/${String(o.whatsapp).replace(/\D/g,'')}">واتساب</a>`:''}</div></div></div></section><section class="section"><h2>عقارات المكتب</h2><div class="cards">${d.properties.map(p=>`<article class="property"><a href="/property.html?id=${p.id}"><img src="${esc(p.image_url||'/placeholder.jpg')}" onerror="this.style.display='none'"></a><div class="property-body"><b>${esc(p.title)}</b><div class="facts">${esc(p.city)} · ${esc(p.district||'')} · ${p.area||'—'} م² · ${p.rooms||'—'} غرف</div><div class="price">${Number(p.price||0).toLocaleString('ar')} $</div></div></article>`).join('')||'<div class="error">لا توجد عقارات منشورة.</div>'}</div></section><section class="section"><h2>فريق المكتب</h2><div class="staff">${d.staff.map(s=>`<div class="person"><b>${esc(s.name)}</b><div class="facts">${esc(s.office_title||'مسوق عقاري')}</div>${s.phone?`<a href="tel:${esc(s.phone)}">${esc(s.phone)}</a>`:''}</div>`).join('')}</div></section>`}catch(e){$('#app').innerHTML='<div class="error">تعذر تحميل صفحة المكتب.</div>'}}load();
