# إدارة الإعلانات والمشاركة

صفحة `/my-listings.html` تعرض الإعلانات التي يستطيع الحساب إدارتها، مع البحث والتقسيم إلى صفحات. تظهر أزرار المشاركة والتعديل والحذف كذلك على بطاقات العقارات وعروض المكاتب والإقامات وصفحات التفاصيل.

- المدير الكامل يدير جميع الإعلانات.
- المعلن يدير إعلاناته، وصاحب المكتب يدير إعلانات مكتبه. الانتماء إلى مكتب وحده لا يمنح الموظف تعديل إعلانات زملائه.
- المشرف المحدود يحتاج `offers.edit` للتعديل أو `offers.delete` للحذف. صلاحية المراجعة أو الإضافة وحدها لا تمنحهما. تستمر صلاحيات `properties.write` و`hotels.write` ضمن قسمها.
- التعديل يشمل بيانات الإعلان الأساسية والموقع والتواصل وشروط الإقامة بحسب القسم؛ تبقى إدارة الوسائط والغرف في شاشاتها القائمة.
- تعديلات صاحب المكتب على عرض مصدر تُعيده للمراجعة. تعديل البيانات لا يمنح اعتمادًا أو يغيّر مالك الإعلان.
- الحذف يتطلب تأكيدًا منفصلًا، ويخفي الإعلان مع الاحتفاظ بالسجل والوسائط والحجوزات. تحفظ قاعدة البيانات `deleted_at` وتمنع عودته للنشر نتيجة مزامنة أو تغيير الحالة من واجهة قديمة. لا توجد استعادة في هذه النسخة.
- تسجَّل عمليات التعديل والحذف في سجل المراجعة. تُرفض العملية إذا تغيّرت بيانات الإعلان منذ فتح نافذته.
- المشاركة متاحة للزوار: تطبيقات الهاتف إذا كانت مدعومة، واتساب، ونسخ الرابط. تستخدم رابط الإعلان العام فقط، دون معاملات الصفحة الحالية أو بيانات الجلسة. إنشاء خيارات المشاركة لا يرسل أي رسالة تلقائيًا.

التحقق: `node --test tests/listing-management.test.js tests/offer-review.test.js tests/limited-admin.test.js tests/property-media.test.js tests/mobile-hotels.test.js`

تشمل الاختبارات صلاحيات الخادم، فصل التعديل والحذف، رفض تغيير الملكية وحالة النشر، منع التغييرات المتعارضة، الحذف مع حفظ العلاقات، عدم إعادة نشر المحذوف، إعادة تطبيق مخطط قاعدة البيانات، تأكيد الحذف وإلغاؤه، نماذج التعديل، والمشاركة وبديل نسخ الرابط.
