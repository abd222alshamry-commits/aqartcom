# صوت سول

تحديث صفحة `/sol.html` لإملاء السؤال بالعربية وسماع الرد.

- اضغط **تحدث إلى سول**، امنح إذن الميكروفون، ثم راجع النص واضغط **إرسال**. لا يُرسل السؤال تلقائيًا.
- اضغط **اسمع الرد** أسفل إجابة سول، أو فعّل **قراءة الردود تلقائيًا**.
- **إيقاف الصوت** يوقف الميكروفون والنطق. يتوقف الصوت أيضًا عند مغادرة الصفحة أو إخفائها وبدء محادثة جديدة.
- تتاح خيارات العربية السعودية والسورية والمصرية للإملاء؛ دقة اللهجة تعتمد على خدمة الجهاز.

## الاتصال والخصوصية

المعالجة المحلية هي الوضع الافتراضي. يتطلب الإملاء المحلي دعم `SpeechRecognition.processLocally` وحزمة عربية مثبتة، ولا يتوفر على كل جهاز أو متصفح. يظهر زر تنزيل الحزمة فقط إذا أبلغ المتصفح بإمكان تنزيلها؛ لا يبدأ التنزيل تلقائيًا. تشغيل نموذج سول المحلي لا يثبت حزمة الإملاء.

إذا لم يتوفر الإملاء المحلي، يمكن للمستخدم تفعيل **السماح بخدمات الصوت عبر الإنترنت عند الحاجة**. عندها قد يرسل المتصفح الصوت لخدمة التعرف، أو نص الرد لخدمة النطق. هذا الخيار غير محفوظ ولا يُفعّل تلقائيًا. لا يسجل الموقع ملفات صوتية ولا يرفعها إلى خادم عقاراتكم. تبقى محادثة النموذج ومعالجة النص كما كانت.

قراءة العربية دون اتصال تحتاج صوتًا عربيًا محليًا مثبتًا على الجهاز. تُفضّل الأصوات المحلية ولا يُستبدل الصوت العربي بصوت إنجليزي. إذا لم يتوفر صوت عربي، تظهر إرشادات بدل الادعاء بأن النطق جاهز. بعض المتصفحات تمنع التشغيل التلقائي؛ يبقى زر **اسمع الرد** متاحًا.

في متصفح غير داعم، تبقى الكتابة متاحة ويمكن استخدام إملاء لوحة مفاتيح الهاتف. هذا تحديث للموقع؛ لا يضيف محرك صوت أصليًا إلى ملفات APK القديمة.

## التحقق

`node --test tests/sol-voice.test.js tests/sol.test.js`

تشمل الاختبارات الموافقة على الخدمات الشبكية، الإملاء المحلي، رفض الإذن، إلغاء الطلب المعلق، النتائج المتأخرة، انقطاع الاتصال، تقسيم الردود الطويلة، اختيار الصوت العربي، وتجربة الردود الفعلية من دليل سول. خدمات الميكروفون والنطق محاكاة في الاختبارات؛ يلزم اختبار سمعي على هاتف حقيقي لتقييم وضوح الصوت ودقة الإملاء.

تُحفظ وحدة الصوت مع ملفات الصفحة في service worker إصدار `aqartkom-v93-sol-shell-v3`، مع الحفاظ على ملفات نموذج سول المحفوظة.

مراجع الواجهات: [SpeechRecognition](https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition)، [المعالجة المحلية](https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition/processLocally)، [الأصوات المحلية](https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisVoice/localService).
