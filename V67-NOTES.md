# عقارتكم V67 — AI Fraud & Trust Center

- مركز ثقة ومكافحة احتيال داخل لوحة الإدارة.
- درجة ثقة 0–100 للعقار والحساب.
- إشارات: تكرار العقار، شذوذ السعر، نقص الصور/الوصف/الموقع، نقص بيانات المالك، مكتب غير موثق، وسجل رفض الحساب.
- تصنيف المخاطر: low / medium / high / critical.
- الفحص لا يرفض عقاراً ولا يوقف حساباً تلقائياً؛ القرار البشري مطلوب.
- API: `GET /api/admin/trust`, `POST /api/admin/trust/scan`, `PATCH /api/admin/trust/:id/status`.
