# V82 — AI Property Matchmaker

- يفهم طلب العميل العربي ويحوّله إلى تفضيلات بحث منظمة.
- يطابق العقارات النشطة حسب البيع/الإيجار، النوع، المدينة، الحي، الميزانية، المساحة والغرف.
- Match Score من 0–100 مع أسباب واضحة لكل نتيجة.
- يستفيد من مؤشرات السوق الداخلية V35/V79 عند توفرها، ولا يخترع معلومات عقارية.
- يدعم غرض الاستثمار ويرفع أولوية الفرص ذات المؤشرات السوقية الأقوى.
- يحفظ جلسات المطابقة ونتائجها للمستخدم.
- Handoff مباشر إلى CRM المكتب كمصدر `ai_matchmaker` لاستكمال المعاينة والتفاوض والصفقة.
- لا يوجد تمويل أو رهن عقاري في V82.

## API
- `POST /api/property-matchmaker/match`
- `GET /api/property-matchmaker/me`
- `POST /api/property-matchmaker/:sessionId/handoff`
