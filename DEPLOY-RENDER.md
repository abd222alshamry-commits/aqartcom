# نشر V93 على Render

- شغّل Dockerfile من جذر المشروع.
- اربط خدمة PostgreSQL عبر متغير DATABASE_URL باستخدام الرابط الداخلي لخدمتين في المنطقة نفسها.
- أرفق قرصًا دائمًا عند /var/data. يحتاج ذلك خدمة مدفوعة على Render.
- تحفظ مجلدات uploads وprivate_uploads وbackups تحت هذا القرص عبر روابط داخل الحاوية.
- مسار التحقق: /api/health.
- لم يتم نشر هذا الإصدار على Render بعد. تم التحقق من صلاحية الكتابة إلى GitHub، وهذه نسخة مصدر للإعداد وليست إثبات جاهزية الإنتاج.
- نجحت الاختبارات الستة وفحص صياغة JavaScript. لم يُختبر بدء التشغيل على PostgreSQL حقيقي بعد.
- لا تتضمن الحزمة بيانات دخول أو مفاتيح إنتاج. اضبط حساب المدير ومتغيرات الخدمات المطلوبة بشكل منفصل.
